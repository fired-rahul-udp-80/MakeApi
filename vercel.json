{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build" // Replace 'build' with your actual output directory
      }
    }
  ]
}
